// libs
import makeStyles from '@material-ui/core/styles/makeStyles'

export const useStyles = makeStyles({
  root: {
    fontSize: '48px',
    fontWeight: 300,
    lineHeight: '1.5em',
    margin: '0 0 16px',
    textAlign: 'center',
  },
})
