// libs
import makeStyles from '@material-ui/styles/makeStyles'

export const useStyles = makeStyles({
  logo: {
    flex: 1,
    fontWeight: 400,
    margin: 0,
  },

  mobileLogo: {
    fontWeight: 300,
    fontSize: '1em',
  },
})
